# TODO

- how do you lock down localdev so only owner can use it?
- how to install plugins by default
- how to save extensions after stop/restart
- Fix machine-exec to use default container
- can we use wif to autologin into google as the user?
-